{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate --no-input && python3 manage.py shell -c \"from apps.accounts.models import User; User.objects.filter(email='pb496189@gmail.com').exists() or User.objects.create_superuser(email='pb496189@gmail.com', password='BHati123@', first_name='PREET', last_name='BHATI', role='admin')\" && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}